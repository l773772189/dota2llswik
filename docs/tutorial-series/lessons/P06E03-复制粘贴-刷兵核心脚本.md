# P06E03：复制粘贴——刷兵核心脚本

> 📋 复制粘贴 wave_spawner.lua，它负责"读取波次配置并按计划刷出敌人"

## 这节课你将完成
- ✅ 将刷兵脚本复制到正确的文件位置
- ✅ 理解这个脚本的作用：它读取你之前写的波次配置，按时间表刷出敌人
- ✅ 确认脚本和配置文件正确关联

## 操作步骤

### 第一步：理解 require 文件名必须匹配

> **重要概念**：在 `addon_game_mode.lua` 中写了 `require("wave_spawner")`，这意味着游戏会去找一个叫 `wave_spawner.lua` 的文件。**文件名必须和 require 里写的名字完全一致**（不含 `.lua` 后缀）。如果名字不匹配，游戏会报错找不到文件。

### 第二步：创建刷兵脚本文件
<!-- ⚠️ 待验证：文件名和 require 路径需实际操作确认 -->
<!-- TODO: 在 scripts/vscripts/ 目录下创建 wave_spawner.lua
  - 再次强调文件名要和 addon_game_mode.lua 中 require 的名字一致 -->

> ✅ **检查点**：你在 `scripts/vscripts/` 目录下创建了 `wave_spawner.lua` 文件

### 第三步：📋 复制粘贴刷兵脚本代码
<!-- ⚠️ 待验证：Lua API 调用需实际操作确认 -->
<!-- TODO: 提供完整的 wave_spawner.lua 代码，按以下功能段分别标注：

  【功能段 A：配置读取】
  - 读取波次配置文件（KV 文件）
  - 解析每波的单位类型、数量、间隔

  【功能段 B：定时器】
  - 设置波次间隔定时器
  - 每隔一段时间触发下一波

  【功能段 C：单位生成】
  - 在指定出生点创建敌人单位
  - 按配置的间隔逐个刷出

  【功能段 D：路径移动】
  - 让敌人沿路径点移动
  - 到达终点后攻击玩家基地

  - 每个功能段之间用醒目的分隔注释（如 "=========="）隔开
  - 强调：复制粘贴即可，不需要自己写！ -->

> ✅ **检查点**：你的 `wave_spawner.lua` 文件中有完整的刷兵逻辑代码

### 第四步：理解核心流程（不用记住）
<!-- TODO: 用流程图或步骤列表解释刷兵脚本的工作流程
  1. 游戏开始 → 读取波次配置
  2. 倒计时结束 → 开始第 1 波
  3. 按配置的间隔刷出敌人
  4. 本波刷完 → 等待下一波
  5. 重复直到所有波次结束
  - 强调：你只需要知道它"读配置、刷敌人"就够了 -->

> ✅ **检查点**：你能用一句话描述这个脚本的作用——"它读取波次配置，按时间刷出敌人"

## 下一课预告
下一课我们将回到配置文件，调整波次节奏，让游戏更好玩！
